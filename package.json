{
  "name": "workflow-engine-bun",
  "version": "0.1.0",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "lint": "tsc --noEmit",
    "test": "bun test",
    "migrate": "bun run src/sql/migrate.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "hono": "^4.10.3",
    "json-logic-js": "^2.0.5",
    "postgres": "^3.4.3",
    "ulid": "^2.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/json-logic-js": "^2.0.8",
    "@types/json-schema": "^7.0.15",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
